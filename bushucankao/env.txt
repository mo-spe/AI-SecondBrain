# ============================================================
# AI-SecondBrain 生产环境配置文件
# ============================================================
# 说明：
# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 不要将 .env 文件提交到代码仓库
# ============================================================

# ============ 服务器配置 ============
# 时区
TZ=Asia/Shanghai

# ============ 数据库配置 ============
# MySQL root 密码（请修改为强密码，至少 16 位包含大小写字母、数字、特殊字符）
MYSQL_ROOT_PASSWORD=YourSecureMySQLRootPassword123!

# MySQL 数据库名称
MYSQL_DATABASE=second_brain

# MySQL 用户名
MYSQL_USER=secondbrain

# MySQL 用户密码（请修改为强密码）
MYSQL_PASSWORD=YourSecureMySQLPassword456!

# MySQL 端口（宿主机端口，生产环境建议不使用默认端口）
MYSQL_PORT=3306

# ============ Redis 配置 ============
# Redis 密码（请修改为强密码）
REDIS_PASSWORD=YourSecureRedisPassword789!

# Redis 端口（宿主机端口）
REDIS_PORT=6379

# ============ Elasticsearch 配置 ============
# Elasticsearch 端口（宿主机端口）
ES_PORT=9200

# Elasticsearch JVM 内存设置（根据服务器内存调整，建议不超过物理内存的 50%）
ES_JAVA_OPTS=-Xms1g -Xmx1g

# ============ Kafka 配置 ============
# Kafka 端口（宿主机端口）
KAFKA_PORT=9092

# Zookeeper 端口（宿主机端口）
ZOOKEEPER_PORT=2181

# ============ 后端配置 ============
# 后端服务端口（宿主机端口）
BACKEND_PORT=8080

# JVM 内存设置（根据服务器内存调整）
JAVA_OPTS=-Xms512m -Xmx1024m -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# ============ 前端配置 ============
# 前端服务端口（宿主机端口）
FRONTEND_PORT=80

# ============ Nginx 配置 ============
# Nginx HTTP 端口
NGINX_HTTP_PORT=80

# Nginx HTTPS 端口
NGINX_HTTPS_PORT=443

# 你的域名（例如：example.com）
DOMAIN_NAME=your-domain.com

# 是否启用 HTTPS（true/false）
ENABLE_HTTPS=true

# ============ AI 服务配置 ============
# AI 提供商（qwen/deepseek/openai）
AI_PROVIDER=qwen

# 通义千问 API 密钥（必填）
QWEN_API_KEY=your_qwen_api_key_here

# 通义千问 API 地址
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 通义千问模型
QWEN_MODEL=qwen-plus

# 通义千问 Embedding API 地址
QWEN_EMBEDDING_URL=https://dashscope.aliyuncs.com/api/v1/services/embeddings/text-embedding/text-embedding

# 通义千问 Embedding 模型
QWEN_EMBEDDING_MODEL=text-embedding-v2

# DeepSeek API 密钥（可选）
DEEPSEEK_API_KEY=

# DeepSeek API 地址
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# DeepSeek 模型
DEEPSEEK_MODEL=deepseek-chat

# OpenAI API 密钥（可选）
OPENAI_API_KEY=

# OpenAI API 地址
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 模型
OPENAI_MODEL=gpt-3.5-turbo

# OpenAI Embedding API 地址
OPENAI_EMBEDDING_URL=https://api.openai.com/v1/embeddings

# OpenAI Embedding 模型
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# ============ DeerFlow 配置 ============
# DeerFlow 服务端口
DEERFLOW_PORT=8000

# DeerFlow API 超时时间（秒）
DEERFLOW_API_TIMEOUT=300

# ============ 阿里云 OSS 配置 ============
# OSS Endpoint（根据实际区域修改）
ALIYUN_OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com

# OSS AccessKey ID
ALIYUN_OSS_ACCESS_KEY_ID=your_access_key_id

# OSS AccessKey Secret
ALIYUN_OSS_ACCESS_KEY_SECRET=your_access_key_secret

# OSS Bucket 名称
ALIYUN_OSS_BUCKET_NAME=thesecondbrain

# ============ 邮件配置 ============
# 是否启用邮件服务（true/false）
MAIL_ENABLED=false

# SMTP 服务器地址
MAIL_HOST=smtp.qq.com

# SMTP 端口
MAIL_PORT=587

# 邮箱用户名
MAIL_USERNAME=

# 邮箱密码
MAIL_PASSWORD=

# ============ 监控配置 ============
# 是否启用 Prometheus 监控（true/false）
PROMETHEUS_ENABLED=false

# Prometheus 端口
PROMETHEUS_PORT=9090

# ============ 日志配置 ============
# 日志级别（DEBUG/INFO/WARN/ERROR）
LOG_LEVEL=INFO

# 日志文件路径
LOG_PATH=/var/log/ai-second-brain

# 日志文件最大大小（MB）
LOG_MAX_FILE_SIZE=100

# 日志文件保留天数
LOG_MAX_HISTORY=30
