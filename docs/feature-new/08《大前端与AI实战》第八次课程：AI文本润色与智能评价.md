好的，这是为您的《大前端与AI实战》实训课程设计的第八次课程的详细内容。这次课程将把我们项目的AI能力推向一个新的高度，真正实现“费曼学习法”的智能反馈闭环。

---

### **《大前端与AI实战》第八次课程：AI文本润色与智能评价**

**课程主题：** 你的私人学习教练：集成大模型进行文本处理与评分
**总时长：** 4学时 (约3-3.5小时教学，半小时答疑与休息)

#### **一、 本次课程目标 (Objectives)**

在本次课程结束后，每位同学都应该能够：
1.  **理解** 大语言模型（LLM）的基本工作原理和通过API调用的方式。
2.  **掌握** 提示词工程（Prompt Engineering）的基础技巧，能设计出结构清晰、指令明确的Prompt。
3.  **在Node.js后端** 调用百度千帆大模型（如文心一言ERNIE-Bot）的API。
4.  **实现** 两个核心AI功能：
    *   **AI文本润色**：将语音转录后可能存在的口语化、不通顺的文本，优化为书面语。
    *   **AI评价打分**：对比原始知识点和用户的复述文本，从多个维度（如准确性、完整性、流畅性）进行评价并给出一个综合分数。
5.  **在前端页面** 上清晰地展示AI的润色结果、评价和分数。
6.  **（可选）将评价结果** 与知识点的状态（如`status`, `reviewList`）关联起来。

#### **二、 核心关键词 (Keywords)**

*   大语言模型 (LLM - Large Language Model)
*   百度千帆 / 文心一言
*   API 调用 (Server-to-Server)
*   提示词工程 (Prompt Engineering)
*   角色扮演 (Role-Playing)
*   上下文学习 (In-Context Learning)
*   JSON 输出约束

---

### **三、 详细教学流程 (Step-by-Step Guide)**

---

#### **第一部分：大模型与Prompt工程入门 (约45分钟)**

**教师讲解：**

1.  **回顾与承接**
    *   “上节课，我们成功地将用户的口头复述转换为了文字。现在我们有了一份学生的‘答卷’（转录文本），但它可能充满了‘嗯’、‘啊’这些口头语，或者逻辑有些跳跃。更重要的是，我们如何判断这份答卷的好坏？这就是今天大语言模型（LLM）要登场的舞台。”

2.  **大语言模型（LLM）是什么？**
    *   **类比：** “把它想象成一个读过人类几乎所有书籍、知识渊博的‘最强大脑’。它不只是一个搜索引擎，它能理解、推理、总结、创作。我们通过对话（即发送Prompt）的方式来让它完成各种任务。”
    *   **我们用的模型：** “我们将使用百度千帆平台上的文心大模型（比如ERNIE-Bot-turbo），它速度快、成本低，非常适合我们的应用场景。”

3.  **核心魔法：提示词工程 (Prompt Engineering)**
    *   **讲解：** “和LLM交互的质量，几乎完全取决于你提问的质量。Prompt就是你给模型的‘指令说明书’，写得越好，它给你的结果就越符合预期。”
    *   **基础技巧介绍：**
        *   **角色扮演 (Role-Playing):** “请你扮演一个严格的计算机科学教授...” -> 这能让模型进入特定情境，输出更专业的回答。
        *   **明确指令 (Clear Instructions):** 清晰地告诉模型要做什么。例如：“请润色以下文本，使其更书面化，修正语法错误，但不要改变核心意思。”
        *   **提供上下文/示例 (Context/Few-shot Learning):** 给出原始知识点和学生复述，让它基于这两部分信息进行判断。
        *   **指定输出格式 (Output Formatting):** “请以JSON格式返回你的评价，必须包含`score`（0-100的数字）、`evaluation`（字符串评价）、`strengths`（优点数组）和`weaknesses`（弱点数组）这四个字段。” -> 这对于程序解析至关重要！

---

#### **第二部分：后端集成文心大模型 (约75分钟)**

**教师带领学生后端编码：**

1.  **准备工作：百度千帆平台**
    *   **操作指引：**
        1.  登录百度智能云控制台，进入“千帆大模型平台”。
        2.  如果首次使用，可能需要开通服务。
        3.  在“应用接入”中创建应用（如果之前语音识别的应用可以复用，则跳过），获得 `API Key` 和 `Secret Key`。
        4.  在“模型仓库”中，找到想要使用的模型，比如 **ERNIE-Bot-turbo**，确保它在你的应用可调用列表中。

2.  **更新后端环境**
    *   在后端 `.env` 文件中添加千帆的凭证（可能与语音识别的相同，也可能不同，取决于应用创建方式）。
        ```.env
        # ...
        QIANFAN_API_KEY=your_qianfan_api_key
        QIANFAN_SECRET_KEY=your_qianfan_secret_key
        ```
    *   **安装千帆SDK：** (百度为大模型提供了新的SDK或调用方式，我们直接使用`axios`来演示更通用的REST API调用方法，这更具普适性)
        *   我们将使用 `axios` 直接调用其REST API，无需额外安装特定SDK。

3.  **创建AI评价控制器**
    *   在后端的 `controllers/baiduAiController.js` 文件中，添加新函数。
    ```javascript
    // controllers/baiduAiController.js
    const axios = require('axios');
    // ... AipSpeechClient and other code ...

    // --- 大模型相关 ---

    // 获取AccessToken的函数
    async function getQianfanAccessToken() {
        const url = `https://aip.baidubce.com/oauth/2.0/token?grant_type=client_credentials&client_id=${process.env.QIANFAN_API_KEY}&client_secret=${process.env.QIANFAN_SECRET_KEY}`;
        try {
            const response = await axios.post(url);
            return response.data.access_token;
        } catch (error) {
            console.error('Error getting Qianfan Access Token:', error.response.data);
            throw new Error('Failed to get Access Token');
        }
    }

    // AI润色与评价的核心函数
    exports.evaluateFeynmanAttempt = async (req, res) => {
        const { originalContent, transcribedText } = req.body;

        if (!originalContent || !transcribedText) {
            return res.status(400).json({ msg: 'Original content and transcribed text are required.' });
        }

        try {
            const accessToken = await getQianfanAccessToken();
            // ERNIE-Bot-turbo的API地址
            const url = `https://aip.baidubce.com/rpc/2.0/ai_custom/v1/wenxinworkshop/chat/eb-instant?access_token=${accessToken}`;

            // --- 精心设计的Prompt ---
            const prompt = `
            你是一个严格而友善的计算机科学学习教练。你的任务是评估学生对一个知识点的复述，并给出反馈。

            【原始知识点】:
            \`\`\`
            ${originalContent}
            \`\`\`

            【学生的口头复述文本】:
            \`\`\`
            ${transcribedText}
            \`\`\`

            请你完成以下三项任务:
            1.  **文本润色**: 将学生的复述文本润色成一段通顺、专业、书面化的文字。修正明显的语法错误和口语化表达，但保持其核心观点不变。
            2.  **综合评价**: 基于原始知识点，对学生的复述进行评价。指出其优点和可以改进的地方。
            3.  **评分**: 综合考虑准确性、完整性、逻辑性和流畅性，给出一个0到100的整数分数。

            请严格按照以下JSON格式返回你的结果，不要包含任何额外的解释或文字。
            {
              "polishedText": "这里是润色后的文本",
              "evaluation": "这里是你的综合评价",
              "strengths": ["优点1", "优点2"],
              "weaknesses": ["可以改进的地方1", "可以改进的地方2"],
              "score": 85
            }
            `;

            const response = await axios.post(url, {
                messages: [
                    {
                        role: 'user',
                        content: prompt
                    }
                ]
            });
            
            // 解析LLM返回的JSON字符串
            const llmResult = JSON.parse(response.data.result);
            res.json(llmResult);

        } catch (error) {
            console.error('Error calling LLM API:', error.response ? error.response.data : error.message);
            res.status(500).send('Server error during AI evaluation.');
        }
    };
    ```

4.  **添加新路由**
    *   在 `routes/audio.js` (或新建一个 `ai.js`) 中添加路由。
        ```javascript
        // routes/audio.js or routes/ai.js
        // ...
        const { evaluateFeynmanAttempt } = require('../controllers/baiduAiController');

        router.post('/evaluate', auth, evaluateFeynmanAttempt);
        // ...
        ```
    *   **注意：** 确保在 `index.js` 中正确使用了这个路由文件。

---

#### **第三部分：前端展示AI反馈 (约75分钟)**

**教师带领学生前端编码：**

“后端已经能提供智能反馈了，现在我们要把这些宝贵的信息优雅地展示给用户。”

1.  **改造 `FeynmanRecordPage.jsx`**
    *   **添加状态：** 我们需要新的 state 来存储AI的反馈结果。
        ```jsx
        // src/pages/FeynmanRecordPage.jsx
        // ...
        const [aiFeedback, setAiFeedback] = useState(null);
        const [isEvaluating, setIsEvaluating] = useState(false);
        // ...
        ```
    *   **触发AI评价：** 当语音转录成功后，自动调用评价API。
        *   修改 `uploadAudio` 函数，在拿到转录文本后，调用新的函数。

        ```jsx
        // 在 uploadAudio 函数的 finally 块之前
        if (response.data.result) {
            setTranscribedText(response.data.result);
            // 触发AI评价
            getAiEvaluation(response.data.result); 
        }

        // ...

        // 新增一个函数来处理AI评价
        const getAiEvaluation = async (transcribed) => {
            setIsEvaluating(true);
            setAiFeedback(null);
            try {
                // 获取原始知识点内容
                const kpResponse = await apiClient.get(`/knowledge-points/${id}`);
                const originalContent = kpResponse.data.content;
                
                const feedbackResponse = await apiClient.post('/audio/evaluate', {
                    originalContent: originalContent,
                    transcribedText: transcribed
                });

                setAiFeedback(feedbackResponse.data);

            } catch (error) {
                console.error('获取AI评价失败', error);
            } finally {
                setIsEvaluating(false);
            }
        };
        ```
        *   **注意：** 为了在 `uploadAudio` 函数中调用 `getAiEvaluation`，需要将 `getAiEvaluation` 的定义放在 `uploadAudio` 之前，或者使用 `useCallback` 进行包装。

2.  **渲染AI反馈结果**
    *   在页面的 JSX 部分，添加新的渲染逻辑。

    ```jsx
    // ... 在转录结果 div 下方
    <hr />
    <h2>AI 教练反馈:</h2>
    {isEvaluating && <p>AI教练正在批阅您的答卷...</p>}
    {aiFeedback && (
        <div className="ai-feedback" style={{ display: 'flex', gap: '2rem' }}>
            <div style={{ flex: 1 }}>
                <h3>AI 润色后的文本</h3>
                <p style={{ background: '#eef', padding: '1rem' }}>{aiFeedback.polishedText}</p>
                
                <h3>综合评价</h3>
                <p>{aiFeedback.evaluation}</p>

                <h3>优点 👍</h3>
                <ul>
                    {aiFeedback.strengths.map((item, index) => <li key={index}>{item}</li>)}
                </ul>

                <h3>待改进 👇</h3>
                <ul>
                    {aiFeedback.weaknesses.map((item, index) => <li key={index}>{item}</li>)}
                </ul>
            </div>
            <div style={{ flex: '0 0 150px', textAlign: 'center' }}>
                <h3>综合得分</h3>
                <div style={{ fontSize: '3rem', fontWeight: 'bold', color: aiFeedback.score > 80 ? 'green' : 'orange' }}>
                    {aiFeedback.score}
                </div>
            </div>
        </div>
    )}
    ```

---

#### **第四部分：联调与扩展 (15分钟)**

1.  **全流程联调：**
    *   确保后端服务、前端服务都已重启并加载最新代码。
    *   走一遍完整流程：登录 -> 选择知识点 -> 复述 -> 停止录音。
    *   观察流程：先显示转录文本，然后显示“AI教练正在批阅...”，最后展示出完整的润色、评价和分数。
    *   **排错：** 如果出错，重点检查：
        *   后端能否成功获取 Access Token。
        *   Prompt 格式是否正确。
        *   LLM 返回的是否是合法的 JSON 字符串。
        *   前端的 API 请求是否正确发送了 `originalContent` 和 `transcribedText`。

2.  **功能扩展讨论 (作业方向)**
    *   **教师提问：** “我们现在拿到了分数，这个分数有什么用？能不能让它影响我们的学习流程？”
    *   **引导学生思考：**
        *   可以在获取到 `aiFeedback` 后，再发送一个请求到后端。
        *   例如：`PUT /api/knowledge-points/:id/update-status`
        *   后端根据 `score` 来更新知识点的状态。如果 `score < 60`，自动将该知识点的 `reviewList` 字段设为 `true`，让它进入复习列表。
        *   这部分可以作为课后作业，让学生自己实现这个闭环。

---

#### **五、 课堂总结与作业**

*   **总结：**
    *   “今天我们完成了‘费曼学习平台’最核心、最智能的部分！我们学会了如何与强大的大语言模型对话，通过精妙的Prompt Engineering，让AI扮演了学习教练的角色。现在，我们的平台不再仅仅是记录和转录，它能够提供深度的、个性化的反馈。这正是AI赋能教育的魅力所在。大家亲手实现了一个完整的智能应用闭环，非常了不起！”
*   **课后作业：**
    1.  **必须完成：** 确保AI评价打分的全流程能够顺畅运行，并且前端能美观地展示所有反馈信息。
    2.  **核心功能闭环（挑战）：** 实现上一节讨论的功能。当AI评分低于某个阈值（比如60分）时，自动调用后端API，将该知识点的 `reviewList` 状态更新为 `true`。并在Dashboard页面上，对需要复习的知识点进行特殊标记（比如加一个红点或不同的背景色）。
*   **预告下次课内容：**
    *   “我们的平台现在功能非常强大，但我们如何应对学生自己的知识库？如何让AI在我们上传的特定文档（比如一篇PDF论文）中进行问答？下次课，我们将进入一个更高级的AI领域——**RAG（检索增强生成）**，学习如何构建基于私有知识库的智能问答Agent！”