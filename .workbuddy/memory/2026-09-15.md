# 2026-09-15

## AI-SecondBrain 项目完整性审查

产出报告：`PROJECT_COMPLETENESS_AUDIT.md`（项目根目录）

### 审查结论
项目**不算完整可用**。后端代码质量高（零 TODO、零空实现占位），但拦路问题是：
1. 前端 4 个写好的页面未注册路由、零引用：KnowledgeGraph.vue / RagQA.vue / Chat.vue / Report.vue
   → 知识图谱、RAG 问答、学习报告、对话管理在 UI 上不可达
2. 数据库初始化断裂：docker-compose.yml:20 挂载路径 `./complete_database_schema_verified.sql` 错误
   （实际在 sql/ 下），且该脚本只有 12 表 vs 39 个 Entity，无 Flyway，V3~V9 需手动跑
3. Knowledge.vue:1068 调 `knowledgeAPI.batchDelete()` 但 api/knowledge.js 未定义 → 必 TypeError
4. ReviewReminderServiceImpl:83/97/109 三个方法只有 log，复习提醒链路空转
5. 导出功能后端(ExportController 5端点)+api/export.js 齐全，前端零入口，只弹"开发中"

### 已核实完整实现（勿重复怀疑）
- RBAC 多租户 + AdminController（10 端点）
- 知识广场 SquareController（12 端点）、协作分享 ShareController
- 游戏化 GamificationServiceImpl（三表联查）
- 工作区题目池：后端 ReviewCardController:271-332 + ReviewCardServiceImpl:112-114 写 pool/副本，
  前端 Review.vue 双 Tab + joinPool，**全链路打通**（PRD 标"设计中"但实际已完成）
- 采集入库工作流重构（待确认 Tab 已落地）
- 用户级 AI 模型选择

### 项目环境注意
- **端口没有 bug**（首轮误判已更正）：application.yml 默认 8081（本地 + vite 代理），
  application-prod.yml 8080（Docker compose），两套配置各自自洽
- PRD 文档状态普遍滞后：RBAC/知识广场/协作分享标"草稿/待评审"，代码其实已完成
- application-local.yml:33 硬编码 sk- 密钥，疑似泄露，需提醒用户轮换

### 工具环境
该环境 Bash 的 coreutils（ls/mkdir/dirname/rm/tail/head）不可用，PowerShell 无输出返回。
- 文件操作统一用 Read/Write/Edit/Glob/Grep；删文件只能用 PowerShell Remove-Item
- `npm` shim 依赖 bash → 跑不了；替代：`cd frontend && node node_modules/vite/bin/vite.js build`
- node 可用（v22.22.2），可 `node --check` 校验 JS 语法
- JDK/Maven 不在 PATH → **后端 Java 改动无法在本机编译验证**，需提醒用户本地跑 mvn

## 身份建档（同日晚）

用户确认：称呼他 **龙行天下**，沟通方式保持「直接给结论、不铺垫」。

已更新/创建：
- `~/.workbuddy/IDENTITY.md` — 名字 WorkBuddy（简称 WB）、Vibe 直接、emoji 🧠
- `~/.workbuddy/USER.md` — 龙行天下 + 项目上下文 + 沟通偏好
- `~/.workbuddy/SOUL.md` — Vibe 段新增「默认说人话，先给结论」「报忧要早」两条
- `~/.workbuddy/MEMORY.md` — 新建，跨项目长期记忆
- 已删除 `~/.workbuddy/BOOTSTRAP.md`（建档脚本不再需要）

---

## 第三轮：修复 P0 阻断项（改 8 个文件）

用户决策：① 批量删除 → 后端加批量接口 ② Dashboard `/chat` 卡 → 注册 Chat.vue ③ RagQA → 不注册

### 改动清单
1. `KnowledgeService.java:63` + `KnowledgeServiceImpl.java:160-197` + `KnowledgeController.java:130-144`
   新增批量删除：`@DeleteMapping("/batch")`，先全量校验存在性+权限再统一删，
   `@Transactional(rollbackFor = Exception.class)`，同步清 ES
2. `frontend/src/api/knowledge.js:50-56` 补 `batchDelete(ids)`
3. `router/index.js:101-114` 注册 `/report`→Report.vue、`/chat`→Chat.vue
4. `MainLayout.vue:131` 侧边栏加「学习报告」（Chat 不入侧边栏，与「数据采集」重复）
5. `docker-compose.yml` + `db-compose.yml` SQL 挂载改为有序四件套：
   01-second_brain.sql / 02-V7 / 03-V8 / 04-V9（数字前缀锁字典序）
6. `Chat.vue:347` 删掉重复的 `import useWorkspaceStore`（**跑构建才发现**）

### 验证结果
- `vite build` ✅ 通过（2137 modules），Report/Chat chunk 正常产出
- ⚠️ 后端 Java **未经编译验证**（本机无 JDK/Maven），已人工复核签名与 import

### 推翻的首轮结论（重要）
- **端口确实没 bug**，两套 profile 各自自洽
- **知识图谱并非不可达**：KnowledgeSystem.vue:516-554 已用 echarts `type:"graph"` 渲染
- **Chat.vue 与 Capture.vue 重复**（同为「对话采集」）
- 真正缺失的前端功能**只有学习报告**
- 更严重的是 Dashboard:112/:126 两个 `router.push` 目标是**不存在的路由**，点了跳空白页

### 排查方法沉淀（可复用）
- `vite build` 是发现「孤儿页面」隐性 bug 的**唯一**手段：未被引用的 .vue 永不参与编译
- 判断页面是否重复 → grep 现有页面是否在调同一接口（本次 `/rag/answer`、`/knowledge/relation/graph`）
- MySQL initdb 按文件名字典序执行 → 用数字前缀控顺序，无需重命名源文件
- **子代理结论必须自证**：本次 subagent 误报「题目池前端未接线」(实际已实现) 和「端口不一致」
